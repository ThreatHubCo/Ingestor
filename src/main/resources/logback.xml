<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <!-- Don't print debug logs to console -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>DEBUG</level>
            <onMatch>DENY</onMatch>
            <onMismatch>NEUTRAL</onMismatch>
        </filter>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%level] %logger{1} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Use ASYNC_CONSOLE instead of CONSOLE for high-volume logging -->
    <appender name="ASYNC_CONSOLE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>8192</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <appender-ref ref="CONSOLE"/>
    </appender>

    <!-- Log to a file -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/application.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%level] %logger{1} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="SENTRY" class="io.sentry.logback.SentryAppender">
        <options>
            <sendDefaultPii>true</sendDefaultPii>
            <logs>
                <enabled>true</enabled>
            </logs>
        </options>
    </appender>

    <!-- Disable startup logs -->
<!--    <statusListener class="ch.qos.logback.core.status.NopStatusListener" />-->

    <!-- Only log Jetty warnings -->
    <logger name="org.eclipse.jetty" level="WARN"/>

    <!-- Don't log HikariCP debug messages -->
    <logger name="com.zaxxer.hikari" level="INFO"/>

    <root level="DEBUG">
        <appender-ref ref="CONSOLE"/>
<!--        <appender-ref ref="FILE"/>-->
        <appender-ref ref="SENTRY"/>
    </root>
</configuration>